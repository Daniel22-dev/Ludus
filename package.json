{
  "name": "ludus",
  "version": "1.3.0",
  "description": "LUDUS — dílna výukových her (builder + samostatné enginy)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
