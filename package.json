{
  "name": "ludus",
  "version": "1.14.5",
  "description": "LUDUS — dílna výukových her (builder + samostatné enginy)",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/build.mjs && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
