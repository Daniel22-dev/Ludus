#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {runArchitectureGate} from '../security/garp27/tools/architecture-integrity.mjs';
const expected=String(process.env.GARP27_TRUSTED_POLICY_SHA256||'').trim();const result=runArchitectureGate({projectRoot:'.',artifact:'dist',trustedPolicySha256:expected,requireTrustedPolicy:true});fs.mkdirSync('qa-results',{recursive:true});fs.writeFileSync(path.join('qa-results','garp27-architecture-ci.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({schema:result.schema,decision:result.decision,trustStatus:result.trustStatus,findings:result.findings.length,liveClaim:result.liveClaim},null,2));if(result.findings.length){for(const f of result.findings)console.error(`FAIL ${f.code} ${f.file}: ${f.detail}`);process.exit(1);}
